# Dependencies
node_modules/
package-lock.json
yarn.lock

# Next.js
.next/
out/
.nextjs/
.cache/

# Build outputs
dist/
build/
# TypeScript incremental compile cache — regenerated on every build, so it
# only ever creates diff noise and merge conflicts.
*.tsbuildinfo
build-output.txt

# Environment variables
.env
.env.local
.env.*.local
# Backups and per-host copies. Deliberately not a blanket .env.* so that
# .env.example stays tracked.
.env.bak*
.env.production
.env.development

# Local-only backup of original (pre-cutout) wig product photos
frontend/.wig-originals-backup/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Database
*.db
*.sqlite
*.sqlite3

# Logs
logs/
*.log
npm-debug.log*

# Prisma
.prisma/
51673/
